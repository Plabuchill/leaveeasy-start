# ผลการทดสอบ LeaveEasy

**รันเมื่อ:** 2026-09-20 12:11:34 +0700 ถึง 12:15:16 +0700 (ใช้เวลา 3.6 นาที)
**คำสั่งที่รัน:** `npx playwright test` (ทดสอบกับเว็บจริงบน Firebase Hosting: `https://plabu-a08a6.web.app`)
**ผลรวม:** ✅ ผ่าน 38 · ❌ ไม่ผ่าน 0 · ⏭️ ข้าม 6 (จากทั้งหมด 44 เทสต์)

---

## US-01 — ดูรายการใบลาทั้งหมด (`tests/leave-requests.spec.js`)

| เทสต์ | ทดสอบอะไร | ผล |
|---|---|---|
| หน้ารายการใบลาว่างเปล่า | บัญชีใหม่ไม่มีใบลาเลย ต้องขึ้น "ยังไม่มีใบขอลาในระบบ" ไม่ใช่ตารางว่าง | ✅ ผ่าน |
| ตารางมีคอลัมน์ครบ | ตารางมีคอลัมน์ หัวข้อ/ประเภทการลา/สถานะ/ผู้ขอลา/วันที่ลา | ✅ ผ่าน |
| สถานะแสดงเป็นป้ายสี | badge สีตรงตามค่าสถานะจริง | ✅ ผ่าน |
| กดที่แถวไปหน้ารายละเอียด | คลิกแถวแล้วต้องไปหน้า `leave-request-detail.html` ของใบนั้น | ✅ ผ่าน |

## US-02 — ยื่นใบขอลาใหม่ (`tests/new-request.spec.js`)

| เทสต์ | ทดสอบอะไร | ผล |
|---|---|---|
| ฟอร์มมีช่องครบตามสเปค | หัวข้อ/เหตุผล/ประเภท/วันที่เริ่ม-สิ้นสุด/ปุ่มบันทึก-ยกเลิก | ✅ ผ่าน |
| ประเภทการลาอ่านจาก leaveTypes จริง | dropdown ต้องมีตัวเลือกว่าง + 3 ประเภทจากข้อมูลจริง | ✅ ผ่าน |
| กรอกครบแล้วบันทึก | สร้างใบใหม่จริง สถานะเริ่มต้น "รอพิจารณา" + createdAt อัตโนมัติ กลับหน้ารายการ | ✅ ผ่าน |
| กรอกไม่ครบ | ต้องขึ้นข้อความเตือน ไม่บันทึก ไม่พาออกจากหน้า | ✅ ผ่าน |
| ปุ่มยกเลิก | กลับหน้ารายการโดยไม่บันทึกใบใหม่ | ✅ ผ่าน |

## US-03 — เปิดดูรายละเอียดใบลา (`tests/detail-and-status.spec.js`)

| เทสต์ | ทดสอบอะไร | ผล |
|---|---|---|
| แสดงครบทุกช่อง | หัวข้อ/เหตุผล/ประเภท/วันที่/ผู้ขอลา/สถานะ/วันที่ยื่น + "ยังไม่ได้กำหนดผู้อนุมัติ" ถ้ายังไม่มี | ✅ ผ่าน |
| ยังไม่มีความเห็น | ขึ้นข้อความ "ยังไม่มีความเห็นในใบนี้" ถ้ายังไม่มีใครเขียน | ✅ ผ่าน |

## US-04 — เปลี่ยนสถานะใบลา (`tests/detail-and-status.spec.js`)

| เทสต์ | ทดสอบอะไร | ผล |
|---|---|---|
| employee ไม่เห็นปุ่มอนุมัติ/ไม่อนุมัติ | แม้ในใบของตัวเอง ก็เปลี่ยนสถานะไม่ได้ | ✅ ผ่าน |
| manager กดอนุมัติแล้วสถานะเปลี่ยนถาวร เปลี่ยนต่อไม่ได้อีก | ต้องใช้บัญชี **manager** จริง | ⏭️ **ข้าม — ติดที่:** ไม่มี `MANAGER_EMAIL`/`MANAGER_PASSWORD` ให้ทดสอบ (ระบบไม่ยอมให้ self-signup ตั้ง role เป็น manager เอง ตามสเปค ต้องสร้างบัญชีแล้วไปแก้ `role` ใน Firebase Console เอง) |
| เปลี่ยนเป็นไม่อนุมัติต้องมีความเห็นก่อนเสมอ | ต้องใช้บัญชี **manager** จริง | ⏭️ **ข้าม — ติดที่:** เหตุผลเดียวกับข้างบน |

## US-05 — เขียนความเห็นการอนุมัติ (`tests/detail-and-status.spec.js`)

| เทสต์ | ทดสอบอะไร | ผล |
|---|---|---|
| ส่งข้อความว่างเปล่าไม่ได้ | ต้องขึ้นข้อความเตือน | ✅ ผ่าน |
| ส่งความเห็นสำเร็จ | แสดงชื่อผู้เขียน/ข้อความ/เวลา และอยู่ถาวรหลังโหลดหน้าใหม่ | ✅ ผ่าน |

## US-06 — จัดการประเภทการลา (`tests/leave-types.spec.js`)

| เทสต์ | ทดสอบอะไร | ผล |
|---|---|---|
| คนที่ไม่ใช่ hr เข้าไม่ได้ | employee เปิด `leave-types.html` ตรงๆ ต้องถูกเด้งออก | ✅ ผ่าน |
| หน้าแสดงตารางประเภท + ปุ่มแก้ไข/ลบ | ต้องใช้บัญชี **hr** จริง | ⏭️ **ข้าม — ติดที่:** ไม่มี `HR_EMAIL`/`HR_PASSWORD` (เหตุผลเดียวกับ manager) |
| เพิ่มประเภทใหม่ โผล่ในตาราง + dropdown หน้ายื่นใบลาทันที | ต้องใช้บัญชี **hr** จริง | ⏭️ **ข้าม — ติดที่:** เหตุผลเดียวกับข้างบน |
| แก้ไขชื่อประเภท อัปเดตทันที | ต้องใช้บัญชี **hr** จริง | ⏭️ **ข้าม — ติดที่:** เหตุผลเดียวกับข้างบน |
| เพิ่มประเภทโดยไม่กรอกชื่อ ต้องเตือน | ต้องใช้บัญชี **hr** จริง | ⏭️ **ข้าม — ติดที่:** เหตุผลเดียวกับข้างบน |

## US-07 — ลบใบลาของตัวเอง (`tests/detail-and-status.spec.js`)

| เทสต์ | ทดสอบอะไร | ผล |
|---|---|---|
| มีปุ่มลบเฉพาะตอน "รอพิจารณา" + ต้องยืนยันก่อนเสมอ | | ✅ ผ่าน |
| กดลบแล้วกดยกเลิก ต้องไม่ลบ | | ✅ ผ่าน |
| ยืนยันลบแล้ว ไฟล์หายจริง กลับหน้ารายการ | | ✅ ผ่าน |

## US-08 — ล็อกอินและเห็นเฉพาะใบลาของตัวเอง (`tests/auth-and-acl.spec.js`)

| เทสต์ | ทดสอบอะไร | ผล |
|---|---|---|
| ฟอร์มสมัครสมาชิกครบช่อง | | ✅ ผ่าน |
| ฟอร์มล็อกอินครบช่อง | | ✅ ผ่าน |
| ไม่ล็อกอิน เปิด index.html/leave-requests.html/new-leave-request.html/leave-types.html/leave-request-detail.html ต้องถูกเด้งไปหน้าล็อกอินทุกหน้า (5 หน้า) | | ✅ ผ่าน (ครบ 5/5) |
| สมัครใหม่ได้ role เริ่มต้น employee + ไม่เห็นเมนู "ประเภทการลา" | | ✅ ผ่าน |
| ใบลาใหม่บันทึก requesterId เป็น uid จริง ไม่ใช่รหัสสมมติ | | ✅ ผ่าน |
| employee เปิดใบลาของ u001 (คนอื่น) ตรงๆ ทาง URL ต้องถูกบล็อก | | ✅ ผ่าน |
| รายการใบลาของ employee แสดงเฉพาะใบตัวเอง ไม่มีใบ u001/u003 ปน | | ✅ ผ่าน |

## US-09 — ปุ่ม AI จัดประเภทการลาอัตโนมัติ (`tests/ai-assist.spec.js`)

| เทสต์ | ทดสอบอะไร | ผล |
|---|---|---|
| มีปุ่ม "ให้ AI ช่วยจัดประเภทการลา" | | ✅ ผ่าน |
| ยังไม่พิมพ์เหตุผล กดปุ่ม ต้องเตือนก่อน ไม่เรียก AI จริง | | ✅ ผ่าน |
| AI เลือกประเภทที่มีอยู่จริงสำเร็จ เปลี่ยนค่า dropdown + ป้ายกำกับตรงสเปคเป๊ะ | | ✅ ผ่าน |
| AI ตอบรหัสที่ไม่มีอยู่จริง ต้องขึ้น "จัดให้ไม่ได้" ไม่เปลี่ยนค่าเดิม | | ✅ ผ่าน |
| ระหว่างรอ AI ปุ่มต้องขึ้นสถานะทำงาน กดซ้ำไม่ได้ | | ✅ ผ่าน |
| เรียก AI ไม่สำเร็จ (server error) ไม่ค้าง ยังบันทึกใบลาได้ปกติ | | ✅ ผ่าน |
| เรียก AI นานเกิน 15 วิ ตัดเวลาไม่ค้าง | | ✅ ผ่าน |
| ผู้ใช้แก้ประเภทที่ AI เลือกไว้เองได้เสมอ | | ✅ ผ่าน |

## อื่นๆ

| เทสต์ | ทดสอบอะไร | ผล |
|---|---|---|
| หน้าแรกเปิดได้และมีชื่อระบบถูกต้อง (`tests/example.spec.js`) | สโมคเทสต์พื้นฐาน ยืนยันว่า deploy/config ถูกต้อง | ✅ ผ่าน |

---

## สรุปจุดที่ยังไม่ผ่าน (ทั้งหมดเป็น "ข้าม" ไม่ใช่ "พัง")

ไม่มีเทสต์ไหน **พังจริง** (0 failed) — 6 ตัวที่เหลือ **ข้ามไปเพราะติดสิทธิ์บัญชีเดียวกันหมด**:

> ระบบไม่ยอมให้สมัครสมาชิกเองแล้วได้ role เป็น `manager`/`hr` (เป็นไปตามสเปคตั้งใจ — กันคนยกระดับสิทธิ์ตัวเอง) ต้องสมัครบัญชีผ่านหน้าเว็บจริงก่อน แล้วเข้า Firebase Console ไปแก้ช่อง `role` ของบัญชีนั้นเป็น `manager` หรือ `hr` เอง จากนั้นตั้งค่า environment variable `MANAGER_EMAIL`/`MANAGER_PASSWORD` และ `HR_EMAIL`/`HR_PASSWORD` ก่อนรัน `npx playwright test` ถึงจะปลดล็อก 6 เทสต์นี้ได้ครบ

- US-04 × 2 ตัว (manager กดอนุมัติ/ไม่อนุมัติ)
- US-06 × 4 ตัว (hr จัดการประเภทการลา)
