# BACKLOG — LeaveEasy

งานที่ยังไม่ทำใน Module 2 (สัปดาห์ 6–9) ตามที่ `leaveeasy-spec.md` ระบุไว้ชัดเจนว่าเป็นงานต่อยอด/Module 3 — เก็บไว้ที่นี่กันหลงลืม ไม่ได้แปลว่าเป็นสิ่งที่ลืมทำ

---

## US-10 — ค้นหา กรอง และเรียงลำดับ

**สถานะสเปค:** 🚀 งานต่อยอด (ถ้าทำ Module 2 เสร็จก่อนเวลา) หรือ Backlog Sprint 2

**ในฐานะ** ผู้อนุมัติ **ฉันต้องการ** หาใบลาที่ต้องการเจอในไม่กี่วินาที **เพื่อ** ไม่ต้องเลื่อนหาทีละบรรทัดเมื่อมีใบลาหลายร้อยใบ

- [ ] หน้ารายการมีช่องค้นหาด้วยคำในหัวข้อ
- [ ] มีตัวกรองตามสถานะ และตัวกรองตามประเภทการลา
- [ ] เรียงลำดับใหม่ไปเก่าเป็นค่าเริ่มต้น และสลับเป็นเก่าไปใหม่ได้
- [ ] ค้นแล้วไม่พบ ให้ขึ้นข้อความว่า "ไม่พบใบขอลาที่ตรงกับคำค้น"

## US-11 — แดชบอร์ดสรุป (ต่อข้อมูลจริง)

**สถานะสเปค:** 📦 Module 3 (ใส่ Backlog Sprint 2 — Module 2 ไม่ทำแดชบอร์ดจริง)

> หมายเหตุ: **โครงหน้า `dashboard.html` ทำไปแล้ว** (สัปดาห์ 9) ตามที่สเปคหัวข้อ 4 กำหนดให้มีตั้งแต่สัปดาห์ 6 — ที่ยังไม่ทำคือการ**ต่อข้อมูลจริง**ตามเกณฑ์ด้านล่าง

**ในฐานะ** ฝ่ายบุคคล **ฉันต้องการ** เห็นตัวเลขสรุปในหน้าเดียว **เพื่อ** รู้ทันทีว่าวันนี้มีใบไหนค้างรอพิจารณาอยู่

- [ ] ตัวเลขนับมาจากข้อมูลจริงในฐานข้อมูล (Firestore) ไม่ใช่ข้อมูลสมมติที่พิมพ์ค้างไว้
- [ ] รายการใบลา 5 รายการล่าสุดดึงจากข้อมูลจริง
- [ ] นับเฉพาะที่ผู้ใช้คนนั้นมีสิทธิ์เห็น (ตาม ACL.md — employee เห็นแค่ของตัวเอง)

## US-12 — แนบเอกสารประกอบใบลา

**สถานะสเปค:** 📦 Module 3 (ต้องใช้ Firebase Storage ซึ่งไม่อยู่ใน Module 2)

**ในฐานะ** ผู้ขอลา **ฉันต้องการ** แนบเอกสารประกอบ เช่น ใบรับรองแพทย์ **เพื่อ** ให้หัวหน้าพิจารณาได้โดยไม่ต้องขอเอกสารเพิ่มทีหลัง

- [ ] หน้ายื่นใบลาใหม่มีปุ่มเลือกไฟล์เอกสาร
- [ ] ไฟล์ไปเก็บใน Firebase Storage ที่ตำแหน่ง `leaveRequests/<รหัสใบลา>/<ชื่อไฟล์>`
- [ ] ลิงก์ของไฟล์เก็บไว้ในช่อง `attachmentUrl` ของเอกสารใบลานั้น
- [ ] หน้ารายละเอียดแสดงลิงก์ให้กดเปิดดูได้
- [ ] ไม่แนบไฟล์ก็บันทึกใบลาได้ตามปกติ

---

## งานค้างระยะสั้น (ไม่ใช่ Backlog Module 3 — ทำได้เลยถ้ามีเวลา)

- [ ] **บัญชีทดสอบ manager/hr** — 6 เทสต์ใน `tests/detail-and-status.spec.js` และ `tests/leave-types.spec.js` ยังข้ามอยู่เพราะไม่มีบัญชีที่มี role นี้จริง วิธีปลดล็อก: สมัครผ่านหน้าเว็บจริง 2 บัญชี → เข้า Firebase Console แก้ `role` ใน `users/{uid}` เป็น `manager`/`hr` → ตั้งค่า env var `MANAGER_EMAIL`/`MANAGER_PASSWORD`, `HR_EMAIL`/`HR_PASSWORD` ก่อนรัน `npx playwright test` (ดูรายละเอียดใน `test-results.md`)
- [ ] **คลิปสาธิต 3 นาที** — ตามเกณฑ์ผ่านสัปดาห์ที่ 9 ในหัวข้อ 8 ของสเปค ยังไม่ได้อัด

---

## หัวข้อ 9 ของสเปค — สิ่งที่ตั้งใจไม่ทำใน Module นี้เลย (ไม่ใช่ backlog เพื่อทำทีหลัง)

รายการนี้คัดลอกจาก `leaveeasy-spec.md` หัวข้อ 9 ไว้เตือนตัวเอง — **ห้ามเผลอทำ** แม้จะดูมีประโยชน์:

- ไม่ใช้ framework ใดๆ, ไม่เขียนเซิร์ฟเวอร์เอง, ไม่ใช้ฐานข้อมูลตาราง, ไม่มี pagination
- ไม่มีแจ้งเตือนอีเมล/LINE/SMS, ไม่มีส่งออก Excel/PDF, ไม่มีอนุมัติหลายขั้น
- ไม่คำนวณวันลาคงเหลือ/โควตา, ไม่มีปฏิทินทีม, ไม่เก็บประวัติการแก้ไข
- ไม่มีบทบาทที่ 4 ขึ้นไป, ไม่มีหลายภาษา, ไม่มีโหมดกลางคืน, ไม่มีการชำระเงิน, ไม่มีแอปมือถือ
