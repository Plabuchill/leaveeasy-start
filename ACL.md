# ACL — สิทธิ์การใช้งาน LeaveEasy

ตารางสิทธิ์ของผู้ใช้ 3 บทบาท อ้างอิงจาก [leaveeasy-spec.md](leaveeasy-spec.md) หัวข้อ 2

> ✅ **สถานะปัจจุบัน:** ตารางนี้บังคับจริงแล้วทั้งสองชั้น — ฝั่งหน้าจอ (ซ่อนปุ่ม/เมนูตามบทบาท) และฝั่ง `firestore.rules` (deploy แล้ว) ที่เช็ก role จากโฟลเดอร์ `users` ก่อนอนุญาตอ่าน/เขียนทุกครั้ง

| บทบาท (`role`) | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **พนักงาน** (`employee`) | ยื่นใบขอลาใหม่ · ดูใบลาของตัวเอง · เขียนความเห็นเพิ่มในใบของตัวเอง · ลบใบของตัวเองที่สถานะยังเป็น `รอพิจารณา` | อนุมัติ/ไม่อนุมัติใบลาของตัวเองไม่ได้ (เปลี่ยนสถานะใบลาไม่ได้เลย ไม่ว่าใบใคร) · ดูใบลาของพนักงานคนอื่นไม่ได้ · แก้ไขประเภทการลาไม่ได้ |
| ✅ **หัวหน้า** (`manager`) | ดูใบลาทุกใบ · เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ) · เขียนความเห็นในทุกใบ | ลบใบลาของคนอื่น · แก้ไขประเภทการลา |
| 🧾 **ฝ่ายบุคคล** (`hr`) | ทำได้ทุกอย่างที่หัวหน้าทำได้ · กำหนดผู้อนุมัติให้แต่ละใบ · เพิ่ม/แก้ไข/ลบ ประเภทการลา · ดูแดชบอร์ดสรุปทั้งระบบ | — |

## กติกาเสริมที่ใช้ร่วมกับตารางนี้

- ใบลาที่สถานะเป็น `อนุมัติ` หรือ `ไม่อนุมัติ` แล้ว เปลี่ยนสถานะต่อไม่ได้ ไม่ว่าบทบาทไหน
- การเปลี่ยนสถานะเป็น `ไม่อนุมัติ` ต้องมีความเห็นในใบนั้นอย่างน้อย 1 รายการก่อนเสมอ
- พนักงานคนหนึ่งเปิดใบลาของพนักงานอีกคนไม่ได้ (บังคับจริงแล้วใน `firestore.rules`) ส่วนหัวหน้าและฝ่ายบุคคลเปิดได้ทุกใบ
